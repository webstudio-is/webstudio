import * as process from "process";

export const config = {
  ladleShots: {
    ladleUrl: "ladle-build",
  },
  lostPixelProjectId: "cleiive6c0gchi40em3uzx1xv",
  apiKey: process.env.LOST_PIXEL_API_KEY,
};
