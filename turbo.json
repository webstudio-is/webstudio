{
  "$schema": "https://turbo.build/schema.json",
  "globalEnv": ["AUTH_SECRET", "DATABASE_URL"],
  "pipeline": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["lib/**", "dist/**", "build/**", "api/**", "public/build/**"]
    },
    "checks": {
      "dependsOn": ["^checks"]
    },
    "storybook:build": {
      "dependsOn": ["@webstudio-is/icons#build", "build"]
    },
    "size-test": {
      "dependsOn": ["^build"]
    }
  }
}
