const config = {
  previewPath: "/preview",
  canvasPath: "/canvas",
  designerPath: "/designer",
  dashboardPath: "/dashboard",
  loginPath: "/login",
  logoutPath: "/logout",
};
export default config;
export type Config = typeof config;
