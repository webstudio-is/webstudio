import type { MetaFunction } from "remix";
import { Document } from "@webstudio-is/sdk";

export const meta: MetaFunction = () => {
  return { title: "Webstudio canvas" };
};

export default Document;
