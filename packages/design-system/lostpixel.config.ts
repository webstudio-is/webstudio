import * as process from "process";

export const config = {
  storybookShots: {
    storybookUrl: "packages/design-system/storybook-static",
  },
  lostPixelProjectId: "clee4j8le039xgm0e6ba7sbfd",
  apiKey: process.env.LOST_PIXEL_API_KEY,
};
