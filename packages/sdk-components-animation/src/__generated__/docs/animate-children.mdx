---
title: "Animation Group"
description: "Animation Group component is designed to animate its children."
component: true
base: "animation"
sourcePackage: "@webstudio-is/sdk-components-animation"
sourceExport: "AnimateChildren"
---

# Animation Group

Animation Group component is designed to animate its children.

## Installation

Use the Webstudio component registry item `animate-children`.

In Builder automation, resolve the item from the generated shadcn-compatible registry and use the Webstudio metadata in `meta` for insertion.

## Usage

Use this component through Webstudio's instance, props, styles, resources, and data-source model.

Inspect the registry metadata before creating or editing instances so content model, states, and composition stay valid.

## Composition

Use the following composition to build `AnimateChildren`:

```text
AnimateChildren
|-- AnimateText
|-- StaggerAnimation
`-- VideoAnimation
```

## Examples

No canonical template is currently exported for this component.

## API Reference

### Props

- `action`: type: `animationAction`, control: `animationAction`. Animation Action

### Content Model

- Category: `instance`
- Direct children: `instance`, `@webstudio-is/sdk-components-animation:AnimateText`, `@webstudio-is/sdk-components-animation:StaggerAnimation`, `@webstudio-is/sdk-components-animation:VideoAnimation`

### States

This component does not define custom visual states.

## Accessibility

Follow Webstudio's semantic component model and preserve accessible labels, text, and relationships when editing.

## LLM Guidance

- Prefer semantic Webstudio components and templates over raw HTML embeds.
- Preserve existing instance structure, props, styles, resources, and data bindings when editing.
- Prefer inserting the complete template or composition tree instead of manually creating individual parts.

## References

No external references are currently mapped for this component.
