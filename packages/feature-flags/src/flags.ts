// Only for development, is not supposed to be enabled at all.
export const internalComponents = false;
export const unsupportedBrowsers = false;
export const resourceProp = false;
export const multiplayer = false;
// Queryable Markdown asset resources remain experimental until scale and
// production telemetry validate the rollout thresholds.
export const assetResource = false;
